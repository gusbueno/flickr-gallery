'use strict'

// import {  } from '../constants/ActionTypes'

const initialState = {
  page: 1,
  totalPages: null
}

const gallery = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default gallery
